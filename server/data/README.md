# Server data boundary

The full medication normalization database is not redistributed in this public
artifact because its provenance and redistribution terms require a separate
review. The server can start without the local file, but normalization coverage
will be limited until an authorized database is supplied by the operator.

Do not commit patient records, prescription images, OCR payloads, or provider
exports to this directory.
